import React from "react";
import { OrbitControls } from "@react-three/drei";

type Props = {};

const AnimateWrapper = (props: Props) => {
  return (
    <>
      <OrbitControls />
    </>
  );
};

export default AnimateWrapper;
